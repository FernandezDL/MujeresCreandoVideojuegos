using UnityEngine;

// Este es un script de ejemplo para mostrar la estructura básica de un MonoBehaviour en Unity
// Diana Fernandez
// 25-04-2026
public class MiPrimerScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Copiado");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
